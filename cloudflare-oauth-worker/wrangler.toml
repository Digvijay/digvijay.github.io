name = "decap-oauth-worker"
main = "index.js"
compatibility_date = "2024-01-01"

# Environment variables (set secrets using wrangler secret put OAUTH_CLIENT_SECRET)
[vars]
OAUTH_CLIENT_ID = "YOUR_GITHUB_CLIENT_ID_HERE"
